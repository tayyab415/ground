# Optional analysis API (Cloud Run with ADC). Leave empty for V1 static desk.
# When unset or unreachable, NDVI and Places mill search are shown as gaps.
VITE_ANALYSIS_URL=

# Never put a Google Maps JavaScript API key in the frontend.
# V1 map uses OSM raster tiles. Classic geocoding keys stay server-side only.
