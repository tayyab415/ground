# Leave empty for the public static V1 desk (judges). NDVI/Places then show as gaps.
# Do not point the public build at a sidecar. Do not put GROUND_SIDECAR_TOKEN here.
VITE_ANALYSIS_URL=

# Never put a Google Maps JavaScript API key in the frontend.
# V1 map uses OSM raster tiles. Classic geocoding keys stay server-side only.
