# Leave empty for the public static desk (judges). The dated EE NDVI snapshot is bundled; the sidecar is not called.
# Do not point the public build at a sidecar. Do not put GROUND_SIDECAR_TOKEN here.
VITE_ANALYSIS_URL=

# Never put a Google Maps JavaScript API key in the frontend.
# Map uses OSM raster tiles. Geocode and directions stay server-side ADC only.
